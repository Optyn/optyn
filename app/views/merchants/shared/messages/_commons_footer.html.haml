- unless @message.is_child?
  .control-group
    = f.label :label_ids, "Who should we send this to?", class: 'control-label'
    .controls
      = f.select :label_ids, options_from_collection_for_select(@labels, :id, :name, @message.label_ids(@labels)),
        {}, { id: "message_label_ids", multiple: 'multiple', class: 'chzn-select span6', 'data-placeholder' => 'Please select the receivers of this message'}

= f.hidden_field :send_immediately, value: 1
