%div
  - unless @message.discount_amount.present?
    %p
      == "We are having a special. Here are the details":
  - else
    %p
      == We are offering a #{message_discount_type_text(@message)} off special.
  
  = render partial: "shared/messages/content"      
    
= render partial: "shared/messages/redemption"

%div
  %p
    %strong Fine Print:
    = render partial: "shared/messages/fine_print"