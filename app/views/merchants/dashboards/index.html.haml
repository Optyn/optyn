/Main Body

.header.row-fluid
  .span12
    %h1
      = current_shop.name
      Dashboard
    %p.pad3pc
      Hi
      == #{current_manager.name}.
      !
      Here's what's going on.

  / .span3.new
  /   %a.btn.btn-primary{:href =>merchants_messages_path}
  /     = image_tag "merchant_menu_campaigncenter_icon_white.png", :alt => 'Campaign'
  /     Create New Campaign
.row-fluid.stats.pad3pc
  .span3.leading
    .count
      = @day_count
    .info
      New Connections in last 24 hours
  .span3.box
    .count
      = @week_count
    .info
      New Connections last 7 days
  .span3.box
    .count
      = @month_count
    .info
      New Connections last 30 days
  .span3.box
    .count
      = @total_count
    .info
      Total Active New Connections
%h2
  Your Latest Activity
  
.pad3pc
  %table.table.table-striped
    %thead
      %tr
        %th When
        %th User
        %th Type
        %th More Info
    %tbody
    - @feed.each do |feed_element|
      %tr
        %td
          = time_ago_in_words(feed_element['time']) 
          ago
        %td
          = feed_element['user']
        %td
          = feed_element['type']
        %td
          - if feed_element['action_url'].present?
            %a{:href => feed_element['action_url'],:style => "color:#7D8B8F"} Details
          - else
            = "--"
