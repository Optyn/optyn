.headertext
  / %h3
  /   New Coupon Campaign
  %p
    This is easy! A coupon will get customers excited and get you a 
    %a{:href => "http://wapo.st/18pncMx", :target => "_blank"}
      net return in excess of 15%.
  %p
    Let's get started!


= render partial: 'merchants/shared/messages/commons_header', locals: {f: f, name_label_text: 'Name the Campaign'}

= render partial: 'merchants/shared/messages/content', locals: {f: f}

.control-group.disc
  = f.label :type_of_discount, class: "control-label"
  .controls.checkbox{ 'data-toggle' => 'tooltip', title: :type_of_discount }
    %label.radio.inline
      = f.radio_button :type_of_discount, :dollar_off
      == $ off or
    %label.radio.inline
      = f.radio_button :type_of_discount, :percentage_off
      == % off
    / .btn-group{ 'data-toggle' => 'buttons-radio' }
    /   type_of_discount:
    /   = :type_of_discount
    /   %button.btn.btn-primary{ :type => 'button', 'data-value' => :dollar_off } $ off
    /   %button.btn.btn-primary{ :type => 'button', 'data-value' => :percentage_off } % off

    /   = f.hidden_field :type_of_discount, :id => "type_of_message_value", :value => ""

.control-group
  = f.label :discount_amount, "How much?", class: 'control-label'
  .controls
    = f.number_field :discount_amount, 'data-toggle' => 'tooltip', title: 'The amount of the coupon, in percentage or dollar amount value.'

.control-group
  = f.label :coupon_code, "Coupon Code", class: 'control-label'
  .controls
    = f.text_field :coupon_code

= render partial: 'merchants/shared/messages/date', locals: {f: f, ending_label: "Coupon Expiration"}

.control-group
  .controls
    %label.checkbox
      = f.check_box(:permanent_coupon)
      This coupon does not expire

= render partial: 'merchants/shared/messages/fine_print', locals: {f: f,
fine_print_verbiage: 'You can use this to say things like 1 per customer or One Per order. The less limitations the better.'}

= render partial: 'merchants/shared/messages/optional', locals: {f: f}

= render partial: 'merchants/shared/messages/commons_footer', locals: {f: f}


/ :javascript
/ $( function() {
/   $( '.disc .btn' ).click( function() {
/     var value = $( this ).data( 'value' );
/     $( '#type_of_message_value' ).attr( 'value', value );
/   });
/ });
