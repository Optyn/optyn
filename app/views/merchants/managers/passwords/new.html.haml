#box_login
  .container
    .span12.box_wrapper
      .span12.box
        %div
          .head
            %h4 Forgot your password?
        
        .form
          = form_for(resource, :as => resource_name, :url => merchants_manager_password_path(resource), :html => { :method => :post }) do |f|

            = f.email_field :email, placeholder: "Email", autofocus: true

            = f.submit "Send me reset password instructions", class: "btn"

      %p{class: "already"}
        = link_to "Sign in", new_merchants_manager_session_path 
        |
        = link_to "Sign Up", new_merchants_manager_registration_path