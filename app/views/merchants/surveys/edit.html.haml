%div
  .row
    %h1 Customer Segmentation questionaire
    .form
      = form_for current_survey, as: :survey, url: merchants_survey_path, method: :put, :html => {class: 'form-horizontal'} do |f|
        = render :partial => 'form', :locals => {f: f}