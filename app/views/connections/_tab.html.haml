#div.tab-pane.active#my_connections
  %ul.nav.nav-tabs{}
    %li{class: connections_tab_class('index')}
      = link_to("My connections", connections_path, id: 'my_connections')
    %li{class: connections_tab_class('make')}
      = link_to("Make new connections", make_connections_path, id: 'new_connections')