/ begins navbar
.navbar.navbar-fixed-top
  .navbar-inner
    .container
      %a.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.brand{:href => root_path}
        %img{:alt => "logo", :src => "/assets/logo.png"}/
      .nav-collapse.collapse
        %ul.nav.pull-right
          %li
            %a{:href => root_path} Home
          %li
            %a{:href => "consumerfeatures"} Features
          %li
            %a{:href => "pricing"} Pricing
          %li
            =link_to("Consumer Login", new_user_session_path, class: "btn-header") if !user_present?
          %li
            = link_to("Merchant Login", new_merchants_manager_session_path, class: "btn-header") if !user_present?
          %li
            = link_to("Logout", destroy_user_session_path, class: "btn-header") if user_signed_in?
          %li
            = link_to("Logout", destroy_user_session_path, class: "btn-header") if merchants_manager_signed_in?
          %li
            = link_to("Get Started", destroy_user_session_path, class: "btn-header") if user_signed_out?
      

/ ends navbar