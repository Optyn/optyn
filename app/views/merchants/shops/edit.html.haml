#merchants
  .container
    .span8.edit_page
      %h3
        = image_tag "edit-account-icon.png", :alt => ''
        Edit
        = current_shop.name
        Details
      .form 
        = form_for :shop, url: merchants_shop_path, method: :put, html: {id: 'edit_shop_form'} do |f|
          = render :partial => 'form', :locals => {f: f}