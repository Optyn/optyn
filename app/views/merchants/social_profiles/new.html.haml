= banner('Social profiles')

= render partial: 'merchants/merchant_managers/tabs'

#merchants.pad3pc
  .container-fluid
    .form
      = form_for :social_profile, url: merchants_social_profiles_path, method: :post, html: {id: 'edit_profile_form', class: 'form-horizontal'} do |f|
        = render partial: 'merchants/social_profiles/form', :locals => {:f => f}
