%h1
  Listing Labels for
  = current_shop.name

.span8
  %table.table.table-striped
    %thead
      %tr
        %th Name
        %th Classified users count
        %th Label
        %th Label
    %tbody
    - @labels.each do |label|
      %tr
        %td= label.name
        %td= label.users_count
        %td= link_to 'Edit', edit_merchants_label_path(label)
        %td= link_to 'Destroy', merchants_label_path(label), :method => :delete, :data => { :confirm => 'Are you sure you want to delete this label? You will also loose all the users classified with this label' }

  %br

  = link_to 'New Label', new_merchants_label_path
