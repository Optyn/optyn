#box_sign
  .container
    .span12.box_wrapper
      .span12.box
        %div  
          .head
            %h4 Create your account
            
          = render partial: "users/shared/social_login_buttons"

          .form  
            = form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
              =# devise_error_messages!

              = f.text_field :name, :autofocus => true, :placeholder => "Name"

              = f.text_field :email, :placeholder => "Email"

              = f.text_field :home_zip_code, :placeholder => "Zip Code (Home)"

              = f.text_field :office_zip_code, :placeholder => "Zip Code (Office)"

              = f.password_field :password, :placeholder => "Password"
              
              = f.password_field :password_confirmation, :placeholder => "Confirm Password"

              %div 
                %em * All the fields above are required.
              
              = f.submit "Sign up", :class => "btn"
      %p{:class => "already"}
        = link_to "Sign in", new_session_path(resource_name)
        %strong OR
        = link_to "Forgot your password?", new_password_path(resource_name)
